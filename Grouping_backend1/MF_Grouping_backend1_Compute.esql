

CREATE COMPUTE MODULE MF_Grouping_backend1_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		-- CALL CopyMessageHeaders();
		 CALL CopyEntireMessage();
		RETURN TRUE;
	END;

	CREATE PROCEDURE CopyMessageHeaders() BEGIN
		DECLARE I INTEGER 1;
		DECLARE J INTEGER;
		SET J = CARDINALITY(InputRoot.*[]);
		WHILE I < J DO
			SET OutputRoot.*[I] = InputRoot.*[I];
			SET I = I + 1;
		END WHILE;
	END;

	CREATE PROCEDURE CopyEntireMessage() BEGIN
		--SET OutputRoot = InputRoot;
		DECLARE inRef REFERENCE TO InputRoot.JSON.Data.Aadhar_no;
		IF inRef=123 THEN
			SET OutputRoot.JSON.Data.AadharDetails.Name='madhu';
			SET OutputRoot.JSON.Data.AadharDetails.Address='korutla';
			SET OutputRoot.JSON.Data.AadharDetails.Age=21;
		END IF;
	END;
END MODULE;
